<?xml version="1.0" encoding="utf-8"?>
<!--
  allowBackup="false" is not enough on Android 12 and later: on some devices
  it leaves device to device transfer on. Every domain listed on
  developer.android.com (behavior changes, Android 12) is excluded from both
  channels. Amehrug stores nothing in device protected storage, so the
  device_* domains are not needed. Moving to a new phone goes through an
  encrypted export, roadmap task 8.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
